# 线程三遗留项跟踪清单

更新时间：2026-03-30
分支：cost/dev

## 当前未完成且已排期

说明：以下未完成项的正式跟踪以根目录 [`成本核算平台需求跟踪矩阵.xlsx`](/D:/Desktop/cost_platform/成本核算平台需求跟踪矩阵.xlsx) 为准，本清单只保留线程三视角摘要，避免重复维护。

1. 公式构建器复用能力
- 当前状态：未完成
- 当前实现：规则中心已提供公式输入框、命名空间提示、变量令牌和常用函数快捷插入，但仍属于轻量助手。
- 详细设计对应：`企业级核算平台详细设计.md` 中“规则复制并改条件值、公式规则复用公式构建器”；`企业级核算平台页面原型设计.md` 中“公式构建器弹窗”。
- 计划完成线程：线程三持续整改
- 计划完成时间：2026-04-02 前
- 交付目标：补齐语法预校验、命名空间引用校验、更多公式片段复用能力，并尽量向变量中心构建器复用靠拢。

2. 阶梯命中预演
- 当前状态：已完成
- 当前实现：规则中心已补齐独立“阶梯命中预演”工作区，可录入条件变量与阶梯依据变量样本值，直接返回条件组命中、逐条条件解释、阶梯定位结果和摘要说明。
- 详细设计对应：`企业级核算平台详细设计.md` 中“阶梯命中结果可解释”；`企业级核算平台页面原型设计.md` 中“阶梯工作区要求”。
- 实际完成线程：线程三持续整改前置完成
- 实际完成时间：2026-04-03
- 交付结果：预演口径已与运行链条件判断保持一致，支持表达式条件布尔结果按 `true/1` 统一识别，避免前台预演与试算/追溯解释分叉。

## 可在当前线程闭环

1. 规则中心 Mapper 启动解析报错
- 状态：已闭环
- 位置：`cost_platform_back/ruoyi-system/src/main/resources/mapper/cost/CostRuleMapper.xml`
- 说明：阶梯区间展示文本中直接写入了 `<`，导致 MyBatis 解析 XML 时抛出 `SAXParseException`。现已改为 `&lt;` 转义写法，后端构建通过。

2. 规则治理校验误用追溯表名
- 状态：已闭环
- 位置：`cost_platform_back/ruoyi-system/src/main/resources/mapper/cost/CostRuleMapper.xml`
- 说明：治理预检查原先误查不存在的 `cost_trace_detail`，现已改为数据库设计中的 `cost_result_trace`，与根目录 SQL 设计保持一致。

3. 规则中心 XML 展示字符兼容性
- 状态：已闭环
- 位置：`cost_platform_back/ruoyi-system/src/main/resources/mapper/cost/CostRuleMapper.xml`
- 说明：条件汇总和阶梯区间文案已收敛为兼容性更稳妥的写法，避免特殊字符在 XML、数据库字符集或复制链路中触发隐蔽问题。

4. 规则中心菜单授权初始化
- 状态：已闭环
- 位置：`cost_platform_back/ruoyi-admin/src/main/resources/db/migration/V20260330_005__thread3_rule_tier.sql`
- 说明：已补充管理员角色与线程三菜单/按钮权限的初始化授权，降低迁移后页面菜单不可见的风险。

5. 计价单位标准化下拉
- 状态：已闭环
- 位置：`cost_platform_back/ruoyi-admin/src/main/resources/db/migration/V20260330_006__unit_dict.sql`、`cost_platform_front/src/views/cost/fee/index.vue`、`cost_platform_front/src/views/cost/rule/index.vue`
- 说明：已补充 `cost_unit_code` 字典，并将费用中心“计价单位”改为下拉选择；规则中心改为显式展示当前费用计价单位，避免规则/阶梯再手工维护一套单位口径。

6. 复制规则时旧定价口径未回填
- 状态：已闭环
- 位置：`cost_platform_front/src/views/cost/rule/index.vue`
- 说明：示例/存量规则的 `pricingConfig` 仍存在旧口径字段（如固定费率的 `unitPrice`、固定金额的 `amount`），复制并改条件值时未自动归一化到线程三表单使用的 `rateValue/amountValue`，导致保存时报“固定费率规则必须填写费率值”。现已在前端详情归一化阶段补兼容映射。

7. 规则中心组合定价与按组变量能力
- 状态：已闭环
- 位置：`cost_platform_front/src/views/cost/rule/index.vue`、`cost_platform_back/ruoyi-system/src/main/java/com/ruoyi/system/service/impl/cost/CostRuleServiceImpl.java`、`cost_platform_back/ruoyi-system/src/main/java/com/ruoyi/system/service/impl/cost/CostRunServiceImpl.java`
- 说明：固定费率/固定金额规则已支持“条件组合组 + 组合定价” typed 表单，同一规则内可按命中组合组自动取价，减少为同价条件组合拆多条规则的成本；运行链也已按命中组读取对应价格配置。

## 当前仍需持续验证

1. 线程三 migration 的真实数据库执行验证
- 当前状态：SQL 文件已补齐，但本轮仅完成静态检查和项目构建，未在真实开发库执行回归
- 风险说明：若数据库已有脏数据、主键占用、字典编码冲突或 Flyway 执行顺序与预期不一致，仍可能在环境落库时暴露问题
- 持续修复建议：后续每次联调前优先执行线程三 migration，并记录实际报错、修复动作和最终库内状态

2. 规则中心运行态联调
- 当前状态：前后端构建均已通过，但尚未完成“选费用 -> 新增规则 -> 配条件/阶梯 -> 复制并改条件值 -> 删除治理检查”的完整手工链路验证
- 风险说明：接口参数口径、字典加载、单位下拉回填、表单回填、分页统计和治理抽屉仍可能在真实页面交互时暴露边界问题
- 持续修复建议：后续启动本地服务后，按线程三主链逐步联调，每发现一个运行态问题就同步追加到本清单并闭环

3. 规则与发布/追溯口径的真实联动
- 当前状态：线程三只实现治理预检查，不提前越界实现线程四发布中心和线程五运行链
- 风险说明：当前 `publishedVersionCount`、`traceCount` 的阻断逻辑已经预留，但必须在线程四、线程五恢复对应链路后再做端到端验证
- 持续修复建议：线程四联调发布快照写入，线程五联调结果追溯命中，再回归验证规则停用/删除阻断是否符合业务口径

## 记录约束

1. 本清单只记录线程三相关风险，不提前展开线程四到六的完整实现。
2. 如后续发现文档、数据库设计与现有实现冲突，优先按根目录文档、已完成线程实现和《企业级核算引擎高级设计方案》收敛。
3. 每次新增风险后，需要同时记录“现象、根因、修复位置、验证结果”，避免问题只停留在对话里。
4. 如某项未在当前线程完成，必须同步登记到根目录《成本核算平台需求跟踪矩阵.xlsx》，写明归属线程、目标时间和验收口径。

## 2026-03-30 主链回归补充

1. 新增阶梯规则时子表 sceneId 未继承当前费用所属场景
- 状态：已闭环
- 现象：页面已选中费用并进入“新增规则 -> 阶梯费率 -> 配阶梯 -> 保存”，后端返回“所属场景不能为空”。
- 根因：规则主表 `sceneId` 会在提交前补齐，但 `conditions/tiers` 子表未同步带出 `sceneId`，被 `@Validated` 在进入服务层前直接拦截。
- 修复位置：`cost_platform_front/src/views/cost/rule/index.vue`
- 修复说明：在 `normalizeSubmitData` 中统一为规则、条件、阶梯继承当前费用所属 `sceneId`。
- 验证结果：浏览器实操新增 `STORAGE_KEEP_TIER_RULE_01` 成功，规则总数从 3 增至 4，阶梯规则数从 0 增至 1。

2. 线程三页面主链已完成一次完整手工回归
- 状态：已完成验证
- 覆盖链路：选费用 -> 新增规则 -> 配阶梯 -> 保存 -> 治理检查 -> 删除；复制并改条件值 -> 保存。
- 验证结果：复制保存成功；新增阶梯规则成功；治理检查正确展示条件数、阶梯数、发布引用和追溯命中；删除检查与删除执行成功，测试阶梯规则已清理。
- 当前结论：线程三“规则中心 + 条件/阶梯维护 + 治理检查”主链已打通，可作为进入线程四、五联动前的稳定基线。

3. 阶梯命中预演与组合定价工作区补齐
- 状态：已完成静态验证
- 覆盖链路：规则中心配置条件组合组 -> 维护组合定价 -> 录入样本值执行阶梯命中预演 -> 查看组级/条件级/阶梯级解释。
- 验证结果：前后端构建均已通过；条件组合编辑已改为直接绑定真实表单数据，避免只改到计算副本；表达式条件的布尔返回值已与运行链统一按 `true/1` 识别。
- 当前结论：规则中心基线能力“组合费率/按组变量”和“阶梯命中预演独立工作区”已进入可联调状态，后续优先切回 P0 的发布前检查与批量试算链路。
