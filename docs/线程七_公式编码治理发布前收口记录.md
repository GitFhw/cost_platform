# 线程七：公式编码治理发布前收口记录

更新时间：2026-04-05
分支：`cost/dev`

## 本次收口

- 发布中心预检查新增“公式编码治理”阻断项。
- 公式变量如果缺少 `formulaCode`，或引用了当前场景不存在、未启用的公式资产，预检查会直接阻断发布。
- 公式金额规则如果缺少 `amountFormulaCode`，或引用了当前场景不存在、未启用的金额公式资产，预检查会直接阻断发布。

## 当前口径

- 新配置进入发布链路时，必须先完成公式资产化绑定，不能再以“只存原始表达式”的方式形成新快照。
- 运行链对历史已发布版本仍保留兼容兜底，避免一次性切断旧版本执行。
- 这意味着线程七“全链只按 `formulaCode` 执行”还没有完全闭环，但已经从“运行时兼容”进一步收口到“发布前强约束”。

## 验证

- `mvn -q "-Dmaven.repo.local=D:\Desktop\cost_platform\.m2-manual" -DskipTests package`
- `mvn -q -pl cost_admin -am "-Dmaven.repo.local=D:\Desktop\cost_platform\.m2-manual" "-Dtest=CostPublishControllerManualIT,CostRunControllerManualIT" -DfailIfNoTests=false "-Dsurefire.failIfNoSpecifiedTests=false" test`

## 后续建议

- 下一步继续把变量中心、规则中心、运行链中对历史原始表达式的兼容入口逐步收掉。
- 在发布中心详情页补充该阻断项的产品化说明，帮助业务侧快速定位是哪条变量或规则未完成公式资产绑定。
