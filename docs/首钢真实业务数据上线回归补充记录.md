# 首钢真实业务数据上线回归补充记录

更新时间：2026-04-05

## 本轮收口

- 打通了试算中心 `billMonth` 入参与运行时上下文，首钢场景中依赖账期的季节性补贴、管理费联动公式现在在试算与正式核算中口径一致。
- 新增 `cost_simulation_record.bill_month` 运行字段，并已纳入上线初始化 SQL。
- 试算中心页面补齐账期输入、账期筛选、记录表账期列和详情账期展示，避免真实业务按月份回归时只能走接口、页面却无法带账期。
- 试算统计接口改为与试算列表共用过滤条件，顶部统计卡片现在会跟随 `sceneId/versionId/billMonth/status` 一起收敛，不再出现“列表按账期筛了，统计还是全量”的口径分叉。
- 首钢真实样本自动化回归新增三条关键用例，位置在 [`CostRunControllerManualIT.java`](/D:/Desktop/cost_platform/cost_platform_back/cost_admin/src/test/java/com/ruoyi/web/controller/cost/CostRunControllerManualIT.java)：
  - `shouldCalculateFormulaFeeWithUpstreamDependenciesViaSingleFeeApi`
  - `shouldKeepShougangRealFeeSampleConsistentAcrossAllSingleFeeApis`
  - `shouldKeepShougangRealFeeSampleConsistentAcrossSimulationAndFormalRun`
  - `shouldFilterSimulationListByBillMonthAndExposePersistedBillMonth`

## 回归覆盖

- 发布版本 -> 单费用输入模板 -> `fee/calculate`
- 首钢真实费用 `14` 项逐项单费用模板/单费用取价回归
- 发布版本 -> 单笔试算 -> 正式核算 -> 结果台账 -> 结果追溯详情
- 首钢真实全样本费用 `14` 项金额一致性校验
- 试算记录账期回写与列表过滤口径对齐
- 试算中心页面按账期执行、按账期查询与详情回显

## 验证结果

- `mvn -q -pl cost_admin -am "-Dmaven.repo.local=D:\Desktop\cost_platform\.m2-manual" "-Dtest=CostRunControllerManualIT#shouldCalculateFormulaFeeWithUpstreamDependenciesViaSingleFeeApi+shouldKeepShougangRealFeeSampleConsistentAcrossAllSingleFeeApis+shouldKeepShougangRealFeeSampleConsistentAcrossSimulationAndFormalRun" -DfailIfNoTests=false "-Dsurefire.failIfNoSpecifiedTests=false" test`
- `mvn -q -pl cost_admin -am "-Dmaven.repo.local=D:\Desktop\cost_platform\.m2-manual" "-Dtest=CostExpressionServiceImplTest,CostRunControllerManualIT,CostPublishControllerManualIT,CostGovernanceControllerManualIT,CostAuthorizationManualIT" -DfailIfNoTests=false "-Dsurefire.failIfNoSpecifiedTests=false" test`
- `mvn -q "-Dmaven.repo.local=D:\Desktop\cost_platform\.m2-manual" -DskipTests package`

## 当前结论

- 首钢真实费用主链已从“单费用联调可用”推进到“试算与正式核算全样本一致”。
- 第一阶段对外接口主链已经补到“首钢 `14` 项真实费用逐项单费用模板/单费用计算可回归”，后续改接口时能直接守住真实业务口径。
- 第一阶段按接口调用方式落地时，第三方只要按计费对象整理变量，即可完成首钢场景的单费用计算和正式核算。
- 后续仍需在目标环境补一轮上线初始化验证和业务侧人工回归记录，但当前代码链路已具备继续放量回归的基础。
