# 线程二遗留项跟踪清单

更新时间：2026-03-30
分支：cost/dev

## 可在当前线程闭环

1. 治理抽屉前端报错（`resolveDictLabel` 空值问题）
- 状态：已闭环
- 位置：`cost_platform_front/src/views/cost/scene/index.vue`、`cost_platform_front/src/views/cost/fee/index.vue`
- 说明：函数已支持数组/ref 双输入，避免 `undefined.find`。

2. 线程二主链前后端与菜单字典初始化
- 状态：已闭环
- 说明：费用中心、变量中心、第三方接入变量（测试/预览/刷新入口）已完成，并通过前后端构建验证。

## 建议后续线程承接

1. 第三方接入变量的“真实生产级闭环”
- 当前状态：线程二已实现轻量闭环（接口地址校验、样例预览、刷新入口）
- 后续建议：在线程五/六补齐真实鉴权托管、字段映射持久化、同步任务与失败重试告警
- 原因：涉及任务调度、运行链、告警治理，不建议在线程二引入过重运行依赖

2. 发布快照与治理阻断口径联动
- 当前状态：线程二治理已按 `snapshot_type + object_code` 预留校验
- 后续建议：在线程四发布中心恢复后联调发布写快照逻辑，回归验证费用/变量阻断一致性
- 原因：发布中心链路属于线程四职责边界

3. 核算引擎高级设计已沉淀，待线程三到五正式落地
- 当前状态：已完成《企业级核算引擎高级设计方案》
- 文档位置：`docs/企业级核算引擎高级设计方案.md`
- 后续建议：线程三优先按文档落表达式运行时、规则引擎、阶梯引擎；线程四落快照装载；线程五落试算与正式核算编排
- 原因：该文档已收敛 `charge`、`formula`、`autoCalc` 的优点与新平台文档约束，可作为后续实现蓝图
