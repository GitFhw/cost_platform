P0：当前最优先，直接影响可上线可用性

发布前检查增加“首发无成功试算记录”警告，不阻断发布。
批量试算落地，并与试算解释、输入模板、结果回看统一。
发布中心、试算中心、正式核算、发布快照做一轮全链回归，确保始终按发布快照执行。
修复并收口全局工作场景联动，所有成本页面只跟随场景中心工作场景。
价格策略类型化表单继续收口，固定费率/固定金额必须支持组合定价与按组变量，减少业务面对原始 JSON 的机会。
P1：高优先，直接影响业务维护体验和治理一致性

公式实验室点选配置持久化。
公式模板库与独立公式版本管理。
变量、规则、阶梯、运行链全链强制按 formulaCode 执行。
规则中心组合定价联调与回归验证，确保发布快照、试算解释与正式核算口径一致。
阶梯命中预演独立工作区。
规则中心公式构建器与公式实验室组件复用。
P2：中优先，影响产品化完整度

独立发布审计页。
结构化执行时间线增强。
试算/正式核算结果导出能力继续完善。
共享影响因素模板从内置模板库向可治理模板中心演进。
元数据/字典引用分析和停用影响提示增强。
P3：中长期治理与平台化

第三方接入变量真实执行链闭环：鉴权托管、同步任务、失败重试、缓存刷新、告警。
数据接入层产品化：字段映射、导入模板、接入任务页。
告警中心接通知中心与自动化恢复。
单机线程池向分布式任务治理升级。
性能专项：大数据量分页、索引、批处理与缓存压测。
cost_init.sql 与上线初始化脚本统一整理。


然后推进 P1，这部分会决定业务是否真正能长期维护。
P2/P3 保持在矩阵里持续跟踪，不要散落在多个 Markdown 里。
