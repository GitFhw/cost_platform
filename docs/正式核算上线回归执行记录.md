# 正式核算上线回归执行记录

更新时间：2026-04-02  
工作区：`D:/Desktop/cost_platform`  
分支：`cost/dev`

## 一、执行说明

- 本记录以 [`正式核算上线回归清单.md`](/D:/Desktop/cost_platform/docs/%E6%AD%A3%E5%BC%8F%E6%A0%B8%E7%AE%97%E4%B8%8A%E7%BA%BF%E5%9B%9E%E5%BD%92%E6%B8%85%E5%8D%95.md) 为准。
- 结构化执行台账统一使用 [`go_live_regression_execution_template.xlsx`](/D:/Desktop/cost_platform/docs/go_live_regression_execution_template.xlsx) 维护，模板说明见 [`正式核算上线回归模板说明.md`](/D:/Desktop/cost_platform/docs/%E6%AD%A3%E5%BC%8F%E6%A0%B8%E7%AE%97%E4%B8%8A%E7%BA%BF%E5%9B%9E%E5%BD%92%E6%A8%A1%E6%9D%BF%E8%AF%B4%E6%98%8E.md)。
- 当前先沉淀“代码已具备 / 构建已验证 / 仍待人工回归 / 仍待目标环境验证”四类状态。
- 等后续真实联调和环境验收完成后，继续把本文件补成最终上线验收记录。

状态口径：

- `已具备`：代码链路已落地，且能从实现中确认能力存在
- `已验证`：已完成本地构建或明确校验通过
- `待人工回归`：需启动前后端或联调环境执行实际操作确认
- `待环境验证`：需在目标数据库、目标权限或目标部署环境确认

## 二、回归项执行结果

### 1. 发布快照准备

1. 选择目标工作场景，确认场景状态、账期和默认版本正确
- 当前状态：待人工回归
- 当前说明：功能链已存在，但本轮未做真实页面操作记录。

2. 在发布中心完成一次正式发布，确认版本号、发布时间、发布人和快照摘要可见
- 当前状态：待人工回归
- 当前说明：发布中心已具备，但需要结合真实数据完成一次发布回归。

3. 校验影响因素、规则、价格方案、变量配置均已进入发布快照，且详情抽屉可查看
- 当前状态：待人工回归
- 当前说明：依赖真实发布数据和抽屉联调确认。

### 2. 正式核算提交流程

1. 使用 `JSON 直传` 提交一笔正式核算任务，确认任务可进入任务中心并生成结果
- 当前状态：已具备
- 当前说明：正式核算提交、任务中心、结果台账链路已实现，入口见 [`task/index.vue`](/D:/Desktop/cost_platform/cost_platform_front/src/views/cost/task/index.vue) 和 [`CostRunServiceImpl.java`](/D:/Desktop/cost_platform/cost_platform_back/ruoyi-system/src/main/java/com/ruoyi/system/service/impl/cost/CostRunServiceImpl.java)。

2. 使用 `JSON 直传` 提交一组批量输入，确认任务明细、结果台账和追溯台账正常生成
- 当前状态：已具备
- 当前说明：任务分片、结果台账、追溯台账均已接入批量链。

3. 使用 `导入批次` 创建批次，确认批次台账可查看批次摘要、样例明细和状态
- 当前状态：已具备
- 当前说明：导入批次创建、批次台账、批次详情和任务回填主链已落地。

4. 从导入批次台账回填批次继续提交正式核算任务，确认任务正确引用 `sourceBatchNo`
- 当前状态：已具备
- 当前说明：任务提交表单、详情抽屉和后端任务对象均已保留 `sourceBatchNo`。

### 3. 分片与失败恢复

1. 提交一笔足够触发多分片的正式核算任务，确认任务详情展示分片列表、范围、耗时和成功失败数
- 当前状态：已具备
- 当前说明：任务详情与分片监控已展示分片号、范围、耗时、成功/失败统计。

2. 制造至少一条失败明细，确认任务详情展示失败聚合摘要和高频错误原因
- 当前状态：已具备
- 当前说明：任务详情已展示失败聚合和高频错误摘要，但仍建议做一次真实失败样本回归。

3. 对失败分片执行一次 `重试分片`，确认分片状态、任务统计和任务明细同步刷新
- 当前状态：已具备
- 当前说明：后端分片重试接口与前端重试按钮均已接通。

4. 对失败明细执行一次 `重试`，确认兼容链路仍可用，且不会覆盖其他分片结果
- 当前状态：已具备
- 当前说明：失败明细重试链仍保留；真实覆盖保护仍建议通过人工回归确认。

### 4. 告警与治理

1. 从任务中心进入告警中心，确认自动带入 `sceneId`、`billMonth`、`taskId` 和未关闭状态
- 当前状态：已具备
- 当前说明：任务中心已支持带上下文跳转到告警中心，告警中心可自动带入筛选条件。

2. 任务失败或重试超限时，确认告警台账能看到对应记录，并可执行确认、关闭
- 当前状态：已具备
- 当前说明：告警台账、确认、关闭和任务回跳已实现，建议再做一次真实异常回归。

3. 校验运行快照缓存状态可查询，缓存刷新操作有权限控制且执行后仍以数据库为准
- 当前状态：已具备
- 当前说明：缓存状态查询与缓存刷新入口已存在，按钮权限也已配置。

### 5. 结果与追溯

1. 在结果台账按场景、账期、任务过滤结果，确认金额、对象标识和执行版本正确
- 当前状态：已具备
- 当前说明：结果台账已支持按 `taskId`、场景、账期过滤。

2. 打开结果详情和追溯详情，确认规则命中、变量快照和解释链路可查看
- 当前状态：已具备
- 当前说明：结果详情与追溯解释链已接通。

3. 校验导入批次任务与 JSON 任务在结果台账中的查询口径一致
- 当前状态：已具备
- 当前说明：两类任务最终都进入统一结果台账口径，但建议用一组真实样本做人工确认。

### 6. 权限与菜单

1. 管理员可在左侧菜单看到 `导入批次`、`正式核算`、`结果台账`、`告警中心`
- 当前状态：待环境验证
- 当前说明：代码和迁移脚本已补齐，但是否实际显示仍需在执行 Flyway 后验证菜单数据。

2. 普通角色在未授予 `cost:task:list`、`cost:alarm:list` 等权限时，不应看到对应菜单与操作按钮
- 当前状态：待环境验证
- 当前说明：前端按钮已用 `v-hasPermi` 收口，后端接口也有权限注解；需真实角色环境验证。

3. 校验任务取消、分片重试、告警确认/关闭、缓存刷新均受按钮权限控制
- 当前状态：已具备
- 当前说明：页面按钮和后端接口均已加权限控制，仍建议做一次普通角色回归。

### 7. 上线前构建校验

1. 后端执行 `mvn -q -DskipTests package` 通过
- 当前状态：已验证
- 当前说明：本轮已执行并通过。

2. 前端执行 `npm run build:prod` 通过
- 当前状态：已验证
- 当前说明：本轮已执行并通过。

3. 若上线环境已启用 Flyway，确认线程五与线程六迁移脚本顺序执行成功
- 当前状态：待环境验证
- 当前说明：需在目标数据库实际执行迁移链验证。

## 三、当前结论

基于当前执行记录，正式核算主链的代码能力已经基本具备，构建校验也已通过；但要达到“可上线运行”的结论，还需要补两类证据：

1. 真实环境 Flyway 迁移执行结果
2. 一轮按清单逐项完成的人工联调/回归结果

在这两类证据补齐前，当前建议状态仍为：`接近上线，可进入上线前验收阶段`。

## 四、最小放行条件

以下条件必须同时满足，才可以把状态从“接近上线”改成“可上线运行”：

1. [`go_live_regression_execution_template.xlsx`](/D:/Desktop/cost_platform/docs/go_live_regression_execution_template.xlsx) 中所有 `P0 + 阻塞上线=是` 的用例状态均为“已通过”。
2. 目标环境 Flyway 已实际执行，并确认线程五新增迁移 `V20260402_012 / 013 / 014` 已进入 `flyway_schema_history`。
3. 管理员菜单与普通角色权限回归已完成，至少覆盖导入批次、正式核算、结果台账、告警中心四个入口。
4. 正式核算双输入模式已在真实环境完成一轮人工回归：
   - `JSON 直传`
   - `导入批次`
5. 分片重试、失败聚合、结果追溯、告警回跳至少完成一轮真实问题样本验证。
6. 本地或目标分支构建校验继续保持通过：
   - `mvn -q -DskipTests package`
   - `npm run build:prod`

若任一条件不满足，则当前状态仍应保持为：`接近上线，可进入上线前验收阶段`。
